<%= simple_form_for(@status,:html => { :multipart => true } , html: {class: "form-horizontal"}) do |f| %>
  <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this photo from being saved:</h2>
       <%= form.file_field :avatar %>
      <ul>
      <% @status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>

  <% end %>
  

      <div class ='field'>
     <%= f.label :avatar %> <br />
     <%= f.file_field :avatar %> 
    </div>


  <%= f.input :content %>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>